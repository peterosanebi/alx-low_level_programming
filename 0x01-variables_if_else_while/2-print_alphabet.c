#include <stdio.h>
/**
 *main main code block
 *
 *Return: prints a to z in lower characters
 */

int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
		putchar(letter);
		letter++;
	return (0);

}
